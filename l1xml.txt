<!-- 
    API (Application Programming Interface)
    XML (Extensible Markup Language) 
    eXtensible = Extend our notation
    Markup = Write using Markup 
    Lauguage = Create you own document


    XML is not all the same HTML but both are markup Language
    XML is used to describe the data.
    HTML is used for display the data

    describe = XML designed to describe data and to focus on what data is
    display = HTML was designed to display data and to focus on how data look



    XML must define custom rules (tags) but HTML has pre-defined rules(tags)
    
    XML to enclose name of key withing double quotes (should not use single quotes)

-->
    eg.element
    <markup>Something</markup>

    eg.attribute
    <markup title="info">Something Here</markup>

    Product List                    Root element (Parent element)
        Product                     Child of root element (child element)
            name                        Child element (root node)
            description                 Child element (root node)
            price                       Child element (root node)

    Product List
        Redbull
            Redball Energy Drink
            Energy drink
            1500
            can